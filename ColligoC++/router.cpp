#include "node.hpp"
#include <stdio.h>
#include <vector>
#include <set>
#include <string>

template<typename T>
class User : public Node<T> {
private:
	int target;

public:
	int connect(std::string target)
};

template<typename T>
int User<T>::connect(std::string tgt) {
	//requires ipv4 xxx.xxx.xxx.xxx
}